Learning Python from tute Dude.

github

git config --global user.name "Cdshivak" #for global config github.
git config --global user.email "cdshivak@gmail.com" # for global config email id in github.

git config --global color.ui auto

mandatory commands
>>>git init
>>> git remote add origin https://github.com/Cdshivak/Tute-Dude-Python-Learning.git


git pull origin main

git status

git add . #to add all files in github from vs code

git commit -m "Test Demo push" #to add a message or commit
git push #to push the code into github from local to online

git checkout -b "Add branch title" #to add a branch in github

git checkout "Branch Name" #to switch between branches or switch

git clone "add url from github to clone the repo"
